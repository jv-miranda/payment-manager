{
  "name": "payment-manager-api",
  "version": "1.0.0",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "connectVm": "ssh -v root@159.223.191.120",
    "deploy": "tar -czf - --exclude='.git' --exclude='node_modules' --exclude='.env' --exclude='.vscode' . | ssh root@159.223.191.120 'rm -rf /var/www/payment-manager/* && tar -xzf - -C /var/www/payment-manager' && ssh root@159.223.191.120 'cd /var/www/payment-manager && npm install --production && npx prisma migrate deploy && pm2 restart payment-manager'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jv-miranda/payment-manager.git"
  },
  "author": "João Vitor Miranda",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "express": "^4.21.2",
    "fs": "^0.0.1-security",
    "moment-timezone": "^0.5.48",
    "pg": "^8.14.1",
    "prisma": "^6.5.0"
  },
  "devDependencies": {
    "supertest": "^7.1.0",
    "vitest": "^3.1.1"
  }
}
